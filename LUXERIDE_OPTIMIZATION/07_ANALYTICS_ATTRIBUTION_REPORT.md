# Fase 16 — Analytics & Attribution Report

Estado: **PENDING**. GA4 ya wireado (`NEXT_PUBLIC_GA_MEASUREMENT_ID` en
`app/layout.tsx`), UTM/gclid ya capturados y persistidos en
`bookings.attribution`. Falta: tabla completa de eventos (event/trigger/
parameters/platform/status/test result), y confirmar que no haya tags GA
duplicados entre layout global y páginas de operador (cada empresa puede
tener su propio GA4 Measurement ID en Settings).
